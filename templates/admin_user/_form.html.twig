{{ form_start(form, {'attr': {'class': 'max-w-md mx-auto p-4 bg-white shadow-md'}}) }}
    {{ form_widget(form, {'attr': {'class': 'w-full px-3 py-2 border rounded-md focus:outline-none focus:border-blue-500 mb-4'}}) }}
<button class="w-full bg-blue-500 text-white p-3 rounded-md hover:bg-blue-600 focus:outline-none focus:shadow-outline-blue" type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
